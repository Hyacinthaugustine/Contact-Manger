export interface contactTypes {
  name: string;
  phoneNumber: string;
  email: string;
  description?: string;
}

export interface userSignInDetails {
  userName: string;
  email: string;
  password: string;
}
